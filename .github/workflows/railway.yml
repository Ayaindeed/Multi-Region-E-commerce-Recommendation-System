name: Railway Deployment Info

on:
  push:
    branches: [ main ]

jobs:
  railway-info:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Railway Deployment Guide
      run: |
        echo "🚂 To deploy to Railway:"
        echo "1. Go to https://railway.app"
        echo "2. Connect your GitHub repository"
        echo "3. Railway will auto-detect the railway.toml config"
        echo "4. Set environment variables if needed"
        echo "5. Deploy automatically on push to main"
        echo ""
        echo "📄 Railway config found:"
        cat railway.toml || echo "No railway.toml found"